<div class="strike">
  <h2 class="text-center font-light"><span><slot /></span></h2>
</div>
<style>
  .strike {
    margin: var(--spacing-level-2) auto;
    width: 100%;
    max-width: var(--width-section);
    position: relative;
  }
  .strike span {
    padding: 0 2em;
    background: #fff;
  }
  .strike:before {
    content: "";
    position: absolute;
    top: 0;
    width: 100%;
    height: 100%;
    height: 1px;
    background: rgba(0, 0, 0, 0.25);
    top: 50%;
    z-index: -1;
  }
  @media (max-width: 768px) {
    .strike:before {
      width: calc(100% - 4em);
    }
  }
  @media (max-width: 640px) {
    .strike span {
      padding: 0;
    }
    .strike:before {
      display: none;
    }
  }
</style>
